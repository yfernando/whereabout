
<%= form_for(@invitation) do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this friend request from being send:</h2>

      <ul>
      <% @invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.text_field :to, autofocus: true, class: "form-control", placeholder: "Email" %>

  <div class="actions">
    <%= f.submit "Send Friend Request", class: "actions btn btn-lg btn-primary btn-block" %>
  </div>
<% end %>
